export function Footer() {
    return (
        <footer className="border-t border-line-soft px-4 py-3 text-center text-xs text-ink-faint">
            BGT — risposte citate dal manuale e dal forum, mai inventate.
        </footer>
    );
}
